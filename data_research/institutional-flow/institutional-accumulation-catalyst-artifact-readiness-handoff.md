# Institutional Accumulation — Catalyst artifact readiness handoff

Canonical active routing handoff:
`data_research/institutional-flow/institutional-accumulation-catalyst-artifact-readiness-handoff.md`

This is the durable routing handoff for Institutional Accumulation / Catalyst Pre-positioning. Older detailed round history remains recoverable from git history.

## Frozen project boundaries

- Phase 2 semantic SHA-256 remains `66ddb3bbf99e40bb1babb9e25a5257612a61206d827e273e6fb9b45b9c35e25b`.
- methodology-development identities remain exactly `41`.
- protected `2454` remains motivation-only; its outcomes remain unopened.
- stock/time holdout outcomes remain sealed.
- refreshed development outcome SHA-256 remains `f1c94313a023b420501033b26ce35f90ba8d52c89a0756ce9b6fc42f44a2c59e`.
- refreshed association SHA-256 remains `779e2be6708e6d8bc55062058ede6178e2dd4cf7634621d05ab05228c31e7b68`.
- Withdrawal v6.0-v6.5 remains frozen and is not an Accumulation input.
- no catalyst/outcome association, threshold, score, optimized weighting, model, strategy, production behavior, scheduler, broad-universe rollout, generic-news layer, or outcome-driven tuning is authorized.
- legacy `/mops/web/ajax_t05st01` attempt count remains frozen at exactly `2`.
- present-day API visibility, collection timestamps, and source event dates alone are never historical PIT/value-version proof.

## Closed prerequisite state

### Catalyst artifact reconstruction/readiness

`institutional-accumulation-catalyst-artifact-reconstruction-readiness-v1`
- Prompt A COMPLETE / Prompt B PASS.
- 33 total identities / 0 ready / 33 `not_pit_ready`.
- Node 24 closeout run `34466672001`, job `102836756516`.

### PIT provenance resolution

`institutional-accumulation-catalyst-pit-provenance-resolution-v1`
- Prompt A COMPLETE / Prompt B PASS.
- durable artifact: `data_research/institutional-flow/institutional-accumulation-catalyst-pit-provenance-resolution-v1.json`.
- frozen state remains `identity_count=33`, `pit_ready=0`, `not_pit_ready=33`, `manual_review=0`, `network_collection_used=false`, `source_network_requests=0`, `outcome_blind=true`.
- protected 2454, development outcome, holdout, and catalyst/outcome-association read flags remain false.

### Prospective PIT-safe capture contract

`institutional-accumulation-catalyst-prospective-pit-capture-contract-v1`
- Prompt A COMPLETE / Prompt B PASS.
- closeout/promotion checkpoint `4c782bebffb892540f74d0f5e1bc151065edfb59`.
- append-only immutable raw-response capture, SHA-256 content identity, parser/methodology identity, `pit_known_at=collected_at`, no historical back-imputation, deterministic identical rerun no-op, conflicting same immutable path fail-closed.

### Prospective live-capture canary

`institutional-accumulation-catalyst-prospective-live-capture-canary-v1`
- Prompt A COMPLETE / Prompt B PASS.
- closeout/promotion checkpoint `017ee0fb03b626e7607af10916594f33320c0b48`.
- live run `34481151262`; exactly stocks `1102`, `1104`, `1216`; independent hosted-runner matrix jobs with `max-parallel:1`.
- total repository-controlled source requests exactly 6: one listing plus one verified detail request per stock.
- durable checkpoint commits: `224e5574f0f7adad2d33890f4948388b97685979`, `7122ab57a5a8947b5a7a15bcf223b25caf48145c`, `f71ab6e689ce18d3655fe64ba462555398654703`.
- exactly six accepted immutable snapshots were durably written under `data_research/institutional-flow/official-disclosure-raw/prospective-catalyst-pit/`.

## Closed round — prospective observation audit

Round: `institutional-accumulation-catalyst-prospective-observation-audit-v1`

Status:
- Prompt A: **COMPLETE**
- **Prompt B closeout: PASS**

Pre-Prompt-A durable baseline:
- `017ee0fb03b626e7607af10916594f33320c0b48`.

Operational start head:
- `23b4e9502a60b479bcbaad6384b6530d3af2b1d6`.

Prompt A implementation commits:
- `d467cd0358e649980a2225aa670889727fd846dd` — add deterministic prospective observation auditor.
- `77026bcb2c333bcf2a730594e513b05d52be3f2d` — add fail-closed observation-audit regression.
- `f377eae7ebe4a451fdce30b525149a0d3518d93a` — add durable deterministic audit artifact.
- `998b4242db149c9f61cbbedbc78d506e85ac136b` — extend Node 24 readiness workflow with observation audit + deterministic regeneration check.
- `60a195c3279d70969723baa25be473dbda9d18b9` — Prompt A durable handoff checkpoint.

Concurrent unrelated repository commits within the baseline-to-closeout ancestry were classified as data-only changes, including daily-gainers and TWSE quarterly-quality artifacts. The bounded research-owned paths are the auditor, its test, the audit artifact, readiness workflow extension, and this handoff. No protected research, Wave A/Wave C, outcome, holdout, association, Withdrawal, model/strategy/production/scheduler path changed.

### Prompt B independent verification

The exact Prompt B below was recovered from durable pre-Prompt-A history at `017ee0fb03b626e7607af10916594f33320c0b48`, not from conversation history or the Prompt A summary.

1. Global routing: `docs/agent-prompts/task-routing.json` still has exactly one active task, `institutional-accumulation`, routed to this handoff — **PASS**.
2. Prior live-capture canary has durable Prompt B PASS at the pre-A checkpoint and the six immutable source snapshots remain the observation source set — **PASS**.
3. Historical PIT artifact remains exactly `33 / 0 pit_ready / 33 not_pit_ready / 0 manual_review`, `network_collection_used=false`, `source_network_requests=0`, `outcome_blind=true`, with protected outcome/holdout/association flags unopened — **PASS**.
4. This round made zero MOPS/source-network requests and did not retry the legacy endpoint. Its workflow is contents-read/zero-network and only reads checked-in snapshots — **PASS**.
5. `scripts/audit_institutional_accumulation_catalyst_prospective_observations.js` directly reuses `validateProspectiveSnapshot` and `snapshotRelativePath` from `scripts/institutional_accumulation_catalyst_prospective_pit_capture_contract.js` as the canonical source of truth — **PASS**.
6. Durable machine audit `data_research/institutional-flow/institutional-accumulation-catalyst-prospective-observation-audit-v1.json` exists and reports `6 valid / 0 invalid / 0 conflict`, exactly stocks `1102`, `1104`, `1216`, each `2 total / 1 listing / 1 detail`, 3 listing + 3 detail, 6 unique immutable snapshot IDs, 6 unique response SHA-256 identities, collection range `2026-09-10T13:12:36.863Z` through `2026-09-10T13:17:19.439Z` — **PASS**.
7. Durable audit records exact source paths, immutable snapshot IDs, response SHA-256 values, response byte lengths, parser identity and methodology identity for all six observations — **PASS**.
8. Auditor validates canonical base64 roundtrip and decoded length; the existing PIT validator validates raw-response SHA-256 and immutable identity; auditor additionally enforces `pit_known_at=collected_at`, expected methodology, expected stock/interface, canonical snapshot path, and `historical_back_imputation_allowed=false` — **PASS**.
9. Fixture regression fails closed for malformed JSON, response-hash mismatch, invalid base64/byte length, duplicate/conflicting immutable identity/path, unexpected stock/interface, and invalid PIT metadata — **PASS**.
10. Node 24 workflow `test: institutional accumulation catalyst readiness` run `34484229269`, job `102894333809`, tested SHA `998b4242db149c9f61cbbedbc78d506e85ac136b`, Node `v24.20.0` — **SUCCESS / PASS**.
11. Observed test counts: readiness `1/1`, historical PIT `1/1`, prospective PIT contract `5/5`, live-canary adapter `6/6`, observation audit `7/7`, all PASS — **PASS**.
12. The workflow regenerated the committed audit with `--write`; byte comparison (`cmp`) and `git diff --exit-code` both passed; readiness and historical PIT artifacts were deep-compared before/after and remained unchanged — **PASS**.
13. Baseline comparison `017ee0fb... -> 60a195c3...` shows no protected Phase 2, historical Wave A/Wave C, outcome, holdout, association, Withdrawal, model, strategy, production or scheduler path changed. Additional files in that ancestry are unrelated concurrent data updates and were explicitly classified rather than silently ignored — **PASS**.
14. Audit artifact Git blob identity from its creation/current unchanged content is `bea7a33aa6791b43a7477f7c259649f2228aaa09` — recorded as durable content identity.

Closeout limitation:
- this proves deterministic zero-network auditing of one six-snapshot collection window only;
- it does not establish longitudinal append-only behavior across repeated capture windows;
- it does not authorize broad-universe collection, scheduler/automation, historical backfill/upgrade, catalyst/outcome association, thresholds, scores, models, strategies, or production behavior.

**Prompt B closeout: PASS**

## Current active round

`institutional-accumulation-catalyst-prospective-repeat-capture-canary-v1`

Status:
- Prompt A: **NOT STARTED / ACTIVE**
- Prompt B: **PREREGISTERED / PENDING**

Promotion does not execute Prompt A automatically.

## Next round objective

Establish the first longitudinal/repeated prospective evidence without jumping to automation: perform exactly one additional bounded live capture window for the same three canary stocks, preserve the original six snapshots append-only, and evolve the zero-network audit only as much as needed to represent repeated windows deterministically. This follows Evidence before Automation and Small Evolution: no scheduler, no broad-universe rollout, no historical backfill, and no outcome/association work.

Exact entry points:
- `scripts/institutional_accumulation_catalyst_prospective_pit_capture_contract.js` — canonical immutable snapshot contract.
- `scripts/collect_institutional_accumulation_catalyst_prospective_canary.js` — bounded live collector and request validation.
- `scripts/audit_institutional_accumulation_catalyst_prospective_observations.js` — deterministic observation auditor to evolve for repeated windows.
- `tests/institutional_accumulation_catalyst_prospective_pit_capture_contract.test.js` — immutable contract regression.
- `tests/institutional_accumulation_catalyst_prospective_canary.test.js` — collector zero-network regression.
- `tests/institutional_accumulation_catalyst_prospective_observations.test.js` — audit zero-network regression.
- `.github/workflows/collect-institutional-accumulation-catalyst-prospective-canary.yml` — existing bounded fresh-runner live canary workflow.
- `.github/workflows/test-institutional-accumulation-catalyst-readiness.yml` — Node 24 zero-network gate.
- `data_research/institutional-flow/official-disclosure-raw/prospective-catalyst-pit/` — append-only prospective root; currently six accepted snapshots.
- `data_research/institutional-flow/institutional-accumulation-catalyst-prospective-observation-audit-v1.json` — current six-observation audit baseline.
- `data_research/institutional-flow/institutional-accumulation-catalyst-pit-provenance-resolution-v1.json` — frozen historical 33 limitation evidence.
- `docs/agent-prompts/task-routing.json` — routing source of truth.
- repository-root `AGENTS.md` — paired-prompt, evidence-first and crawl-safety rules.

## Prompt A — bounded prospective repeat-capture canary

```text
Continue the Institutional Accumulation / Catalyst Pre-positioning project only if `docs/agent-prompts/task-routing.json` still routes the sole active task to `data_research/institutional-flow/institutional-accumulation-catalyst-artifact-readiness-handoff.md` and round `institutional-accumulation-catalyst-prospective-repeat-capture-canary-v1` remains Prompt A NOT STARTED / ACTIVE.

Before work:
1. fetch current remote `main`, read repository-root `AGENTS.md`, `docs/project-philosophy.md`, `docs/roadmap/current-phase.md`, routing, this handoff, the PIT contract, collector, auditor, their tests and both relevant workflows;
2. recover this exact Prompt A + Prompt B pair from durable pre-Prompt-A history;
3. independently confirm the observation-audit round has durable Prompt B PASS, the prospective root still contains exactly the original six accepted snapshots, and historical 33/protected state remains frozen;
4. classify any concurrent changes before continuing.

Objective: prove one repeated forward-only capture window for exactly stocks 1102, 1104, 1216 while preserving append-only PIT identity and deterministic auditing. This is longitudinal evidence plumbing only.

Ordered execution:
A. Before any live request, make only the bounded zero-network audit/test change required so the auditor supports repeated observation windows without weakening validation. It must continue to reject unexpected stocks/interfaces, malformed/hash/base64/byte/PIT/path conflicts and must preserve exact source-path/immutable/hash/time reporting. Do not hard-code an unsafe requirement that every raw response hash be unique across windows because identical official content may legitimately recur; immutable snapshot identity/path and collection time remain the append-only observation identity.
B. Run the Node 24 zero-network readiness workflow and require all existing contract/canary/audit regressions to pass before live collection. If it fails, repair only the bounded defect and do not launch source requests until green.
C. Launch exactly one additional live capture window using `.github/workflows/collect-institutional-accumulation-catalyst-prospective-canary.yml` for the existing preregistered stocks 1102, 1104, 1216 only. Keep independent fresh-runner physical jobs, `max-parallel:1`, one listing and at most one verified detail request per stock, randomized production 20-60 second pre-request cooldown, no tight retry, and total repository-controlled source requests <=6.
D. Use only official `POST /mops/api/t05st01` and `POST /mops/api/t05st01_detail`. Never retry `/mops/web/ajax_t05st01`, never run historical range/backfill, and never touch historical Wave A/Wave C.
E. Require race-safe append-only checkpointing. The original six snapshot files must remain byte-identical and present. New accepted snapshots must have valid raw base64 bytes, decoded length, response SHA-256, parser/methodology identity, `pit_known_at=collected_at`, `historical_back_imputation_allowed=false`, canonical immutable path, and remote durable verification. Existing-byte equality may no-op only for the exact same immutable identity; any conflicting immutable identity/path must fail closed.
F. After all three jobs checkpoint durably, regenerate the deterministic observation audit and record the exact post-run counts. Expected successful shape is 12 valid observations, 0 invalid, 0 conflict, exactly the same three stocks, each 4 total / 2 listing / 2 detail, 6 listing + 6 detail, and 12 unique immutable snapshot IDs. Do not require 12 unique response SHA-256 values; report the actual unique raw-content hash count because repeated official content may be identical.
G. Re-run the Node 24 zero-network deterministic regeneration gate on the final checked-in state and require byte-for-byte audit reproduction.
H. Compare the pre-A baseline to final Prompt-A head. Only the bounded auditor/test/workflow adjustments, six-or-fewer new prospective snapshots, regenerated audit, trigger/checkpoint metadata if required, and this handoff may belong to this round. Concurrent unrelated data-only changes must be classified separately.

Frozen/safety rules:
- no scheduler or recurring automation;
- no broad-universe expansion;
- no historical PIT upgrade or historical backfill;
- no development outcomes, holdouts, protected 2454 outcomes, future-return data, catalyst/outcome association, Withdrawal, threshold, score, optimized weighting, model, strategy, production behavior, or generic-news layer;
- zero legacy endpoint retries;
- fail closed on degraded/WAF/security/malformed/application/schema/identity ambiguity before snapshot acceptance;
- do not execute Prompt B automatically.

Completion contract:
- pre-live zero-network gate PASS;
- exactly one bounded second live window completes within <=6 repository-controlled source requests;
- original six snapshots remain unchanged and all new accepted snapshots are durable append-only on current remote main;
- deterministic audit is durably regenerated with the exact observed longitudinal counts and no conflicts;
- final Node 24 zero-network gate PASS at the recorded tested SHA;
- canonical handoff records exact implementation commits, live run/job IDs, request/snapshot counts, checkpoint commits, audit counts/content identity, limitations, and preserves the exact preregistered Prompt B below;
- stop with `Prompt A complete — ready for Prompt B.`
```

## Prompt B — bounded prospective repeat-capture canary closeout

```text
Perform mandatory closeout for `institutional-accumulation-catalyst-prospective-repeat-capture-canary-v1` only after its Prompt A completes. Fetch current remote `main` and recover this exact Prompt B from durable pre-Prompt-A history. Independently verify: sole active routing still points to this handoff; the preceding observation-audit round has durable Prompt B PASS; historical 33 identities remain exactly 0 pit_ready / 33 not_pit_ready and protected state remains unopened; the pre-live Node 24 zero-network gate passed before any new source request; exactly one additional bounded live capture window ran for only stocks 1102, 1104, 1216 through independent fresh-runner jobs with max-parallel 1; total repository-controlled source requests were <=6 with one listing and at most one detail per stock and production 20-60 second pre-request cooldown; only official t05st01/t05st01_detail APIs were used; no legacy endpoint, historical backfill, Wave A/Wave C refetch/rewrite, scheduler, or broad-universe rollout occurred; the original six prospective snapshots remain byte-identical and durable; every newly accepted snapshot is append-only, raw-base64/byte/hash validated, has canonical immutable identity/path, parser/methodology identity, pit_known_at equal to collected_at, historical_back_imputation_allowed=false, and remote durable checkpoint evidence; race-safe checkpoint conflicts fail closed; the evolved deterministic audit correctly represents repeated windows, rejects malformed/hash/base64/byte/PIT/path/stock/interface conflicts, and on a fully successful second window reports 12 valid / 0 invalid / 0 conflict, exactly three stocks with 2 listing + 2 detail each, 12 unique immutable snapshot identities, while reporting rather than requiring uniqueness of repeated raw-response SHA-256 content; final Node 24 zero-network workflow passes at the recorded tested SHA and deterministic regeneration byte-matches the committed audit; compare the bounded changed-file set against the pre-Prompt-A baseline and confirm no protected Phase 2, outcome, holdout, association, Withdrawal, model/strategy/production/scheduler paths changed. Fix only bounded defects and restart verification. On PASS record exact commits/runs/jobs/request counts/checkpoints/audit content identity/limitations, promote only a justified next preregistered round, end `Prompt B closeout: PASS`, and stop without executing the promoted Prompt A.
```

## Stop conditions

- Do not execute the promoted Prompt A automatically.
- Do not reopen or upgrade the historical 33 identities.
- Do not open protected outcomes, holdouts, association, or Withdrawal state.
- Do not mutate frozen Phase 2/outcome/association artifacts.
- Do not introduce thresholds, scores, models, strategies, production behavior, scheduler, broad-universe rollout, or generic-news features.
- Do not retry the legacy endpoint.
- Fail closed whenever PIT/value-version proof, immutable content identity, source response quality, or observation validity is unresolved.
