name: Crawl Institutional Data

on:
  schedule:
    # 每天 17:00 台北時間 (09:00 UTC)
    - cron: '0 9 * * *'
    # 每天 19:00 台北時間 (11:00 UTC)
    - cron: '0 11 * * *'
  workflow_dispatch:
    inputs:
      date:
        description: 'Target Date (YYYYMMDD format, e.g., 20260204)'
        required: false
        default: ''

jobs:
  crawl-institutional:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium

      - name: Run Institutional crawler
        run: |
          if [ -n "${{ github.event.inputs.date }}" ]; then
            node scripts/crawl_institutional_data.js --date ${{ github.event.inputs.date }}
          else
            node scripts/crawl_institutional_data.js
          fi

      - name: Generate files.json
        run: |
          node scripts/generate_file_lists.js || echo "generate_file_lists.js not found, skipping"

      - name: Commit and push changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A

          if git diff --staged --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          CURRENT_DATE=$(TZ='Asia/Taipei' date '+%Y-%m-%d %H:%M:%S')
          git commit -m "🏦 Update Institutional data - ${CURRENT_DATE}"

          for i in 1 2 3; do
            git pull --rebase origin main && git push && break
            echo "Push failed, retry $i..."
            sleep 5
          done
